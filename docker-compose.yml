version: '3.9'

services:
  video-downloader:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: video-downloader-api
    ports:
      - "9001:9001"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
